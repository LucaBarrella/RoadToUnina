{
  "name": "roadtounina",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "npm run build --prefix frontend",
    "dev": "npm run dev --prefix frontend",
    "start": "npm run start --prefix backend",
    "codegen": "npm run codegen --prefix backend",
    "docs:manual": "python3 generate_manual_markdown.py && python3 generate_manual_pdf.py && python3 generate_manual_pdf_dark.py"
  }
}
